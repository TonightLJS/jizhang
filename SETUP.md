# 随手记账 · 云端同步部署指南（Supabase + 任意静态托管，免费）

本项目已内置「本地优先 + 云端同步」能力：数据默认存在你手机浏览器（localStorage），
开启云端后，会额外把数据镜像到 **Supabase**（免费层足够个人用），实现 **多设备同步 + 备份**。
未配置云端时，App 行为与之前完全一致（纯本地）。

---

## 一、创建 Supabase 项目（免费）

1. 打开 https://supabase.com ，用 GitHub 注册并登录（免费）。
2. 新建一个 Project（地区随便选，名字如 `ledger`）。
3. 等待几十秒初始化完成。

## 二、建表（复制粘贴一条 SQL）

1. 进入后台左侧 **SQL Editor** → **New query**。
2. 把本项目根目录的 `supabase_schema.sql` 全部内容粘进去，点 **Run**。
   - 它会建一张 `ledger_sync` 表，并开启 RLS（行级权限），保证每人只能读自己的数据。

## 三、拿到连接信息

1. 后台左侧 **Project Settings** → **API**。
2. 复制两样东西：
   - **Project URL**（形如 `https://xxxx.supabase.co`）
   - **anon public key**（以 `eyJ` 开头的一长串）

> anon key 是公开的前缀密钥，可以放心放在前端；真正的安全由上面的 RLS 策略保证。

## 四、在 App 里填配置（手机/电脑都能填，不用改代码）

1. 打开你的记账 App → 底部「我的」。
2. 顶部新出现的「云端同步（Supabase）」卡片 → 点 **配置云端**。
3. 粘贴 URL 与 anon key → 保存。
4. 点 **登录 / 注册**：同一邮箱 + 密码即可（多设备用同一邮箱登录就自动同步）。
   - 若注册后提示要去验证邮箱：可在 Supabase 后台
     **Authentication → Providers → Email** 取消勾选 *Confirm email*（个人自用小工具推荐关掉），关掉后注册完直接就能用。

## 五、同步怎么用

- 配置并登录后，每次记账/分账/改账户都会 **自动同步（约 0.7 秒后）**。
- 每次同步都是「**先拉取云端 → 按 id 合并 → 再上传**」的完整回合，因此**任何一台设备都不会用本地旧副本覆盖掉另一台新增的记录**。
- 在另一台设备（或清了缓存重装）登录同一邮箱 → 打开 App 会自动拉取并合并数据。
- 「我的 → 云端同步」里可点 **立即同步** 主动同步（比如想马上把手机的数据同步到 iPad）。
- 同步采用「按 id 并集合并」，冲突取 `updatedAt` 较新者，所以**两台设备各自离线记的账，联网后会合并而不是互相覆盖**。
- 默认账本与 4 个默认付款方式使用**固定 id**，多设备各自初始化时不会重复出现两份。
- 已同步时间会显示在卡片里。

## 六、把 App 发布到网上（让任意设备用同一个网址打开）

把整个文件夹当作静态站点部署即可（Supabase 负责云端数据，不依赖特定服务器）：

### 方式 A：GitHub Pages（免费）

> 📖 **手把手图文步骤见 `DEPLOY_GITHUB.md`**（含注册 GitHub、建仓库、推送、开启 Pages、装到手机、后续更新）。

1. 把这个文件夹推到 GitHub 一个仓库。
2. 仓库 Settings → Pages → Source 选 `main` 分支 `/root` → Save。
3. 几分钟后访问 `https://你的用户名.github.io/仓库名/` 即可。
4. iPhone 上用 Safari 打开该网址 → 分享 → 添加到主屏幕，体验等同 App。

### 方式 B：任意静态托管

Netlify / Vercel / Cloudflare Pages / 国内对象存储静态网站 都行，上传本文件夹即可。
注意：Service Worker 要求站点必须是 **HTTPS**（GitHub Pages 等默认就是）。本地 `http://localhost` 调试也行。

---

## 七、常见问题

- **数据会不会丢？** 云端是备份 + 同步；本地也始终保留一份。建议偶尔用「我的 → 数据备份 → 导出」另存一份 JSON。
- **换手机怎么办？** 新手机打开同一个网址（或重装 PWA）→ 登录同一邮箱 → 立即同步，数据就回来了。
- **删除的记录会同步吗？** 当前版本「删除」不会跨设备传播（并集合并只增不改删）。若需在一台设备删了另一台也删，请在另一台也手动删一次。后续可加墓碑机制。
- **一定要用 Supabase 吗？** 不是。不填配置就是纯本地版，和原来一样。Supabase 只是其中一个免费、无需自己搭服务器的选项。
- **费用？** Supabase 免费层：数据库 500MB、存储 1GB、每月 5 万认证用户、足够的 API 调用，个人记账远用不完；超出才按量计费。
- **怎么验证云端真的通了？（可选的自动自检）** 建好项目后，在项目目录执行：
  ```bash
  SUPABASE_URL=https://你的项目.supabase.co SUPABASE_ANON_KEY=你的anonKey node e2e_real_supabase.js
  ```
  它会自动注册一个临时账号，模拟「手机记账 → iPad 登录拉取 → 反向同步」全流程并逐项打印 PASS/FAIL，跑完可在后台删掉该测试账号。（本地还有 `e2e_supabase_test.js`，用内置的 Supabase 兼容后端离线自检，不需要任何凭据。）
