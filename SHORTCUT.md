# 用 iPhone 快捷指令快速记账

目标：**说一句话、点一下图标，就记好一笔账**，不用打开 App、不用点选分类。

原理很短：快捷指令只是「用一条网址打开 App」。网址里带上你要记的那句话，
App 自己会解析出金额、分类、付款方式、日期，然后记账。

网页地址：**https://tonightljs.github.io/jizhang/**

---

## 一、网址长什么样

```
https://tonightljs.github.io/jizhang/?quick=1&text=午饭35&auto=1
```

| 参数 | 作用 | 必填 |
|---|---|---|
| `quick=1` | 打开 App 并进入「快速记账」模式 | ✅ |
| `text=` | 要识别的那句话 | ✅ |
| `auto=1` | **识别成功就直接记账**，不弹面板（推荐，最省事） | 建议加 |
| `type=income` | 强制算作收入（`expense` 支出） | 可选 |
| `cat=food` | 强制指定分类（见附录分类代码） | 可选 |
| `acc=微信` | 强制指定付款方式 | 可选 |
| `date=2026-09-01` | 指定日期（精确到日） | 可选 |
| `time=14:30` | 指定时间（精确到**分钟**，24 小时制） | 可选 |

> 不加 `auto=1` 时，App 会弹出一个面板让你**核对后再记**，比较稳妥，适合刚开始用。

### 能听懂哪些说法

| 你说 | 记成 |
|---|---|
| `午饭35` | 支出 ¥35 · 餐饮 · 今天 · **当前时刻** |
| `打车 12.5 微信` | 支出 ¥12.5 · 交通 · 微信 |
| `昨天超市120 支付宝` | 日期自动变成昨天 |
| `下午3点半 咖啡35` | 时间自动记为 **15:30** |
| `晚上8点 晚饭60` | 时间自动记为 **20:00** |
| `午饭35 12:30` | 时间自动记为 **12:30** |
| `9点15分 打车20` | 时间自动记为 **09:15** |
| `工资8000` | **收入** ¥8000 · 工资 |
| `收到红包200` | 收入 ¥200 · 红包 |
| `房租两千三` | 支持中文数字 → ¥2300 · 居住 |
| `9月1日 房租3000` | 日期识别为 2026-09-01 |
| `奶茶18` `猫砂60` `电影60` `话费100` | 自动落到对应分类 |
| `$20 coffee` / `30美元打车` | 自动识别为美元 |

> 🕐 **时间会精确到分钟**：口语里的「早上/上午/中午/下午/傍晚/晚上/凌晨」都会被换算成 24 小时制；
> 如果那句话里没说时间，就自动记成**你说话的那一刻**，方便以后按时间回看当天流水。

---

## 二、搭建方法（两种，选一种）

### 方法 A：Siri 语音记账（最爽，推荐先做这个）

1. 打开 iPhone 上的**「快捷指令」**App（Shortcuts）。
2. 右上角 **＋** 新建一个快捷指令。
3. 点 **「添加操作」**，搜索框输入 **`听写`**，选择 **「听写文本」**（Dictate Text）。
   - 语言选**中文（普通话）**。
4. 再点 **＋ 添加操作**，搜索 **`网址`**，选择 **「URL」**，在框里填：
   ```
   https://tonightljs.github.io/jizhang/?quick=1&auto=1&text=
   ```
   > ⚠️ 注意末尾的 `=` 要留着，等会儿接听写结果。
5. 在 **「URL」** 操作里，把光标放到 `text=` 后面，点击键盘上方的**变量**按钮，
   插入第 3 步的 **「听写文本」** 变量。
6. 再添加操作 **「打开 URL」**（Open URLs）。
7. 点右上角**命名**，把名字改成 **「记账」**，图标换个喜欢的颜色。
8. 完成！之后**对着手机说**：
   > 「嘿 Siri，记账」
   > （Siri 会提示你说话）你：「打车 35 微信」
   > 手机会在后台打开一下网页，记好后自动切回来。

### 方法 B：主屏一键记账（点图标 → 输入 → 记账）

1. 快捷指令 App → **＋** 新建。
2. 添加操作 **「要求输入」**（Ask for Input），
   - 输入类型选 **文字**
   - 提示语填：`要记什么？例如：午饭35`
3. 添加操作 **「URL」**，填入：
   ```
   https://tonightljs.github.io/jizhang/?quick=1&auto=1&text=
   ```
   然后同样在 `text=` 后面插入 **「要求输入」** 的结果变量。
4. 添加操作 **「打开 URL」**。
5. 长按左上角图标 → **添加到主屏幕**，取名叫「记账」。

以后点桌面图标 → 弹框输入 `午饭35` → 确认 → 完事。

### 方法 C：分享面板（在微信/支付宝账单页直接记）

1. 新建快捷指令，第一个操作添加 **「接收来自快捷指令的输入」**
   （在设置的"快捷指令输入"里，或者用「获取剪贴板」）。
2. 加 **「URL」** 操作，`text=` 后插入输入变量。
3. 加 **「打开 URL」**。
4. 给这个快捷指令打开 **「在分享表单中显示」**，输入类型勾选**文本**。

之后在任意 App 里选中一段文字 → 分享 → 选这个快捷指令，就能把那句话记成账。

### 方法 D：锁屏/桌面小组件

1. 做好方法 A 或 B 的快捷指令后。
2. 长按锁屏 → **自定义** → 添加小组件 → 选**快捷指令** → 选「记账」。
3. 以后锁屏上点一下就记账，连解锁都不用。

### 方法 E：截图/照片里的金额（OCR 记账）

1. 快捷指令开头加 **「从图像中提取文本」**（配合「选择照片」或「拍摄照片」）。
2. 后面接 **「URL」**（`text=` 后插入提取出的文本）→ **「打开 URL」**。

适合拍下小票、账单截图直接记账。

---

## 三、不装快捷指令也能测

App 里 **「我的」页 → 快捷记账 → ⚡ 快捷指令**：
- 可以复制上面那条网址
- 可以直接在输入框里敲一句话，点「识别并记账」看效果

先在电脑浏览器里试试最快：
```
https://tonightljs.github.io/jizhang/?quick=1&text=午饭35&auto=1
```

---

## 四、常见问题

**Q：点了之后白屏 / 没反应？**
A：第一次打开需要联网加载。先在 Safari 里访问一次 `https://tonightljs.github.io/jizhang/`，
等页面正常显示后，快捷指令就都能用了。

**Q：记的账去哪了？**
A：存在**手机本地浏览器**里。用 Safari 打开网页 → 分享 → **添加到主屏幕**，
之后从主屏图标进入就是同一个账本。
如果你启用了**云端同步**（我的 → 云端同步），换设备也能看到。

**Q：能不能不开浏览器、完全静默记账？**
A：不能。纯网页应用必须由系统打开一次网页才能写入数据，这是 iOS 的限制。
但配合 `auto=1`，它几乎是瞬间完成并自动切回，体感上跟静默没区别。

**Q：识别错了怎么办？**
A：**把 `auto=1` 去掉**，这样每次都会弹出核对面板，你可以改完再点「记一笔」。
记错的历史记录也可以随时删除或退款（点那笔记录 → 删除 / 退款）。

**Q：Siri 听错了数字？**
A：去 设置 → Siri 与搜索，确认语言是「普通话」；也可以改用方法 B 手动输入。

**Q：`text=` 里的中文要转码吗？**
A：**不用**。快捷指令会自动编码。手写网址时浏览器也会自动处理。

---

## 附录：分类代码（`cat=` 参数可用）

| 支出 | 代码 | 收入 | 代码 |
|---|---|---|---|
| 餐饮 | `food` | 工资 | `salary` |
| 交通 | `transport` | 奖金 | `bonus` |
| 购物 | `shopping` | 理财 | `invest` |
| 居住 | `home` | 兼职 | `parttime` |
| 娱乐 | `fun` | 红包 | `redbag` |
| 医疗 | `medical` | 其他 | `other_inc` |
| 学习 | `study` | | |
| 通讯 | `phone` | | |
| 旅行 | `travel` | | |
| 宠物 | `pet` | | |
| 人情 | `gift` | | |
| 其他 | `other_exp` | | |

## 附录：一整套可直接复制的网址

```
# 直接记账（不弹窗）
https://tonightljs.github.io/jizhang/?quick=1&auto=1&text=午饭35

# 弹窗核对
https://tonightljs.github.io/jizhang/?quick=1&text=打车12.5 微信

# 强制记成收入
https://tonightljs.github.io/jizhang/?quick=1&auto=1&type=income&text=报销450

# 强制分类 + 付款方式 + 日期
https://tonightljs.github.io/jizhang/?quick=1&auto=1&cat=pet&acc=支付宝&date=2026-09-01&text=猫粮200

# 指定精确到分钟的时间（24 小时制）
https://tonightljs.github.io/jizhang/?quick=1&auto=1&time=14:30&text=午饭35

# 口语时间也行（等价于上一条）
https://tonightljs.github.io/jizhang/?quick=1&auto=1&text=下午3点半 咖啡35
```

---

## 常见问题

**Q：时间怎么记的？会不会影响月度/年度统计？**
A：不会。日期仍然只存到「日」（`YYYY-MM-DD`），时间**单独**存在一个字段里（`HH:MM`）。
所以按月、按日、按账本区间的统计口径**完全不变**，只是明细里多了一列时刻，方便回看「那天几点花的」。

**Q：以前记的老账有时间吗？**
A：会自动补上。老记录没有时间字段，App 会在首次打开时用它的**创建时刻**反推出当时的时分补进去；
如果实在推不出来，就留空，界面上不显示时刻，不影响金额和统计。

**Q：一天记了好几笔，顺序乱吗？**
A：不乱。同一天的流水按**时间从晚到早**排列（最近记的在最上面），和时间轴一致。

**Q：快捷指令里时间框能改吗？**
A：能。不加 `auto=1` 时会弹出核对面板，面板上「日期 / 时间」是两个并排的框，
时间框已经自动填好识别结果（或当前时刻），你可以手动微调后再点「记一笔」。
把时间框清空，就表示这笔**不记时间**。
